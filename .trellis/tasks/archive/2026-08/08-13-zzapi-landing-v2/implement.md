# Implementation Plan

1. Add a deterministic default-landing opening component and lifecycle hook.
2. Mount the default homepage beneath it; isolate all custom-home branches.
3. Refine Hero into a formal zzapi lockup, concise position, and two actions.
4. Recompose Gateway around diagonal ingress, core, and egress paths.
5. Replace V1 animation CSS with brand variables, responsive composition,
   light/dark treatment, and reduced motion.
6. Run focused tests, i18n sync, typecheck, lint/format, build, and visual checks.
7. Review scope and regressions before committing.
